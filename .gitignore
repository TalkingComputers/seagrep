/target
# stray local tooling artifacts (not part of holys3)
agentdb.rvf
agentdb.rvf.lock
ruvector.db
.cursor/

# transient xbench artifacts (keep runs/*.json reports)
crates/xbench/runs/corpus/
crates/xbench/runs/*-cache*/

# local planning scratch, never tracked
/docs/
